export type UserProfileToken = {
  user_id: string;
  username: string;
  email: string;
  access_token: string;
};

export type UserProfile = {
  user_id: string;
  username: string;
  email: string;
};